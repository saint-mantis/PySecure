from _datetime import datetime
#ALL LETTERS IN THE PASSWORD SHOULD BE SMALL ALPHABETS
def covert(password):
    try:
        for i in password:
            while i == "a":
                try:
                    try:
                        date = datetime.now()
                        datee = date.date()
                        hour = date.hour
                        minute = date.minute
                        seconds = date.second
                        mili = date.microsecond
                    except:
                        raise Exception("Could not find DateTime module please import DateTime ")

                    fileopen = open(f"{datee}{hour}{minute}{seconds}{mili}.txt", 'w')
                    filewrite = fileopen.write("1234")
                    fileopen.close()
                    break

                except:
                    raise Exception("error")
        for i in password:
            while i == "b":
                try:
                    try:
                        date = datetime.now()
                        datee = date.date()
                        hour = date.hour
                        minute = date.minute
                        seconds = date.second
                        mili = date.microsecond
                    except:
                        raise Exception("Could not find DateTime module please import DateTime ")

                    fileopen = open(f"{datee}{hour}{minute}{seconds}{mili}.txt", 'w')
                    filewrite = fileopen.write("1234")
                    fileopen.close()
                    break

                except:
                    raise Exception("error")
        for i in password:
            while i == "c":
                try:
                    try:
                        date = datetime.now()
                        datee = date.date()
                        hour = date.hour
                        minute = date.minute
                        seconds = date.second
                        mili = date.microsecond
                    except:
                        raise Exception("Could not find DateTime module please import DateTime ")

                    fileopen = open(f"{datee}{hour}{minute}{seconds}{mili}.txt", 'w')
                    filewrite = fileopen.write("1234")
                    fileopen.close()
                    break

                except:
                    raise Exception("error")
        for i in password:
            while i == "d":
                try:
                    try:
                        date = datetime.now()
                        datee = date.date()
                        hour = date.hour
                        minute = date.minute
                        seconds = date.second
                        mili = date.microsecond
                    except:
                        raise Exception("Could not find DateTime module please import DateTime ")

                    fileopen = open(f"{datee}{hour}{minute}{seconds}{mili}.txt", 'w')
                    filewrite = fileopen.write("1234")
                    fileopen.close()
                    break

                except:
                    raise Exception("error")

        for i in password:
            while i == "e":
                try:
                    try:
                        date = datetime.now()
                        datee = date.date()
                        hour = date.hour
                        minute = date.minute
                        seconds = date.second
                        mili = date.microsecond
                    except:
                        raise Exception("Could not find DateTime module please import DateTime ")

                    fileopen = open(f"{datee}{hour}{minute}{seconds}{mili}.txt", 'w')
                    filewrite = fileopen.write("1234")
                    fileopen.close()
                    break

                except:
                    raise Exception("error")

        for i in password:
            while i == "f":
                try:
                    try:
                        date = datetime.now()
                        datee = date.date()
                        hour = date.hour
                        minute = date.minute
                        seconds = date.second
                        mili = date.microsecond
                    except:
                        raise Exception("Could not find DateTime module please import DateTime ")

                    fileopen = open(f"{datee}{hour}{minute}{seconds}{mili}.txt", 'w')
                    filewrite = fileopen.write("1234")
                    fileopen.close()
                    break

                except:
                    raise Exception("error")

        for i in password:
            while i == "g":
                try:
                    try:
                        date = datetime.now()
                        datee = date.date()
                        hour = date.hour
                        minute = date.minute
                        seconds = date.second
                        mili = date.microsecond
                    except:
                        raise Exception("Could not find DateTime module please import DateTime ")

                    fileopen = open(f"{datee}{hour}{minute}{seconds}{mili}.txt", 'w')
                    filewrite = fileopen.write("1234")
                    fileopen.close()
                    break

                except:
                    raise Exception("error")

        for i in password:
            while i == "h":
                try:
                    try:
                        date = datetime.now()
                        datee = date.date()
                        hour = date.hour
                        minute = date.minute
                        seconds = date.second
                        mili = date.microsecond
                    except:
                        raise Exception("Could not find DateTime module please import DateTime ")

                    fileopen = open(f"{datee}{hour}{minute}{seconds}{mili}.txt", 'w')
                    filewrite = fileopen.write("1234")
                    fileopen.close()
                    break

                except:
                    raise Exception("error")

        for i in password:
            while i == "i":
                try:
                    try:
                        date = datetime.now()
                        datee = date.date()
                        hour = date.hour
                        minute = date.minute
                        seconds = date.second
                        mili = date.microsecond
                    except:
                        raise Exception("Could not find DateTime module please import DateTime ")

                    fileopen = open(f"{datee}{hour}{minute}{seconds}{mili}.txt", 'w')
                    filewrite = fileopen.write("1234")
                    fileopen.close()
                    break

                except:
                    raise Exception("error")

        for i in password:
            while i == "j":
                try:
                    try:
                        date = datetime.now()
                        datee = date.date()
                        hour = date.hour
                        minute = date.minute
                        seconds = date.second
                        mili = date.microsecond
                    except:
                        raise Exception("Could not find DateTime module please import DateTime ")

                    fileopen = open(f"{datee}{hour}{minute}{seconds}{mili}.txt", 'w')
                    filewrite = fileopen.write("1234")
                    fileopen.close()
                    break

                except:
                    raise Exception("error")

        for i in password:
            while i == "k":
                try:
                    try:
                        date = datetime.now()
                        datee = date.date()
                        hour = date.hour
                        minute = date.minute
                        seconds = date.second
                        mili = date.microsecond
                    except:
                        raise Exception("Could not find DateTime module please import DateTime ")

                    fileopen = open(f"{datee}{hour}{minute}{seconds}{mili}.txt", 'w')
                    filewrite = fileopen.write("1234")
                    fileopen.close()
                    break

                except:
                    raise Exception("error")

        for i in password:
            while i == "l":
                try:
                    try:
                        date = datetime.now()
                        datee = date.date()
                        hour = date.hour
                        minute = date.minute
                        seconds = date.second
                        mili = date.microsecond
                    except:
                        raise Exception("Could not find DateTime module please import DateTime ")

                    fileopen = open(f"{datee}{hour}{minute}{seconds}{mili}.txt", 'w')
                    filewrite = fileopen.write("1234")
                    fileopen.close()
                    break

                except:
                    raise Exception("error")
        for i in password:
            while i == "m":
                try:
                    try:
                        date = datetime.now()
                        datee = date.date()
                        hour = date.hour
                        minute = date.minute
                        seconds = date.second
                        mili = date.microsecond
                    except:
                        raise Exception("Could not find DateTime module please import DateTime ")

                    fileopen = open(f"{datee}{hour}{minute}{seconds}{mili}.txt", 'w')
                    filewrite = fileopen.write("1234")
                    fileopen.close()
                    break

                except:
                    raise Exception("error")

        for i in password:
            while i == "n":
                try:
                    try:
                        date = datetime.now()
                        datee = date.date()
                        hour = date.hour
                        minute = date.minute
                        seconds = date.second
                        mili = date.microsecond
                    except:
                        raise Exception("Could not find DateTime module please import DateTime ")

                    fileopen = open(f"{datee}{hour}{minute}{seconds}{mili}.txt", 'w')
                    filewrite = fileopen.write("1234")
                    fileopen.close()
                    break

                except:
                    raise Exception("error")

        for i in password:
            while i == "o":
                try:
                    try:
                        date = datetime.now()
                        datee = date.date()
                        hour = date.hour
                        minute = date.minute
                        seconds = date.second
                        mili = date.microsecond
                    except:
                        raise Exception("Could not find DateTime module please import DateTime ")

                    fileopen = open(f"{datee}{hour}{minute}{seconds}{mili}.txt", 'w')
                    filewrite = fileopen.write("1234")
                    fileopen.close()
                    break

                except:
                    raise Exception("error")

        for i in password:
            while i == "p":
                try:
                    try:
                        date = datetime.now()
                        datee = date.date()
                        hour = date.hour
                        minute = date.minute
                        seconds = date.second
                        mili = date.microsecond
                    except:
                        raise Exception("Could not find DateTime module please import DateTime ")

                    fileopen = open(f"{datee}{hour}{minute}{seconds}{mili}.txt", 'w')
                    filewrite = fileopen.write("1234")
                    fileopen.close()
                    break

                except:
                    raise Exception("error")

        for i in password:
            while i == "q":
                try:
                    try:
                        date = datetime.now()
                        datee = date.date()
                        hour = date.hour
                        minute = date.minute
                        seconds = date.second
                        mili = date.microsecond
                    except:
                        raise Exception("Could not find DateTime module please import DateTime ")

                    fileopen = open(f"{datee}{hour}{minute}{seconds}{mili}.txt", 'w')
                    filewrite = fileopen.write("1234")
                    fileopen.close()
                    break

                except:
                    raise Exception("error")

        for i in password:
            while i == "r":
                try:
                    try:
                        date = datetime.now()
                        datee = date.date()
                        hour = date.hour
                        minute = date.minute
                        seconds = date.second
                        mili = date.microsecond
                    except:
                        raise Exception("Could not find DateTime module please import DateTime ")

                    fileopen = open(f"{datee}{hour}{minute}{seconds}{mili}.txt", 'w')
                    filewrite = fileopen.write("1234")
                    fileopen.close()
                    break

                except:
                    raise Exception("error")

        for i in password:
            while i == "s":
                try:
                    try:
                        date = datetime.now()
                        datee = date.date()
                        hour = date.hour
                        minute = date.minute
                        seconds = date.second
                        mili = date.microsecond
                    except:
                        raise Exception("Could not find DateTime module please import DateTime ")

                    fileopen = open(f"{datee}{hour}{minute}{seconds}{mili}.txt", 'w')
                    filewrite = fileopen.write("1234")
                    fileopen.close()
                    break

                except:
                    raise Exception("error")

        for i in password:
            while i == "t":
                try:
                    try:
                        date = datetime.now()
                        datee = date.date()
                        hour = date.hour
                        minute = date.minute
                        seconds = date.second
                        mili = date.microsecond
                    except:
                        raise Exception("Could not find DateTime module please import DateTime ")

                    fileopen = open(f"{datee}{hour}{minute}{seconds}{mili}.txt", 'w')
                    filewrite = fileopen.write("1234")
                    fileopen.close()
                    break

                except:
                    raise Exception("error")

        for i in password:
            while i == "u":
                try:
                    try:
                        date = datetime.now()
                        datee = date.date()
                        hour = date.hour
                        minute = date.minute
                        seconds = date.second
                        mili = date.microsecond
                    except:
                        raise Exception("Could not find DateTime module please import DateTime ")

                    fileopen = open(f"{datee}{hour}{minute}{seconds}{mili}.txt", 'w')
                    filewrite = fileopen.write("1234")
                    fileopen.close()
                    break

                except:
                    raise Exception("error")

        for i in password:
            while i == "v":
                try:
                    try:
                        date = datetime.now()
                        datee = date.date()
                        hour = date.hour
                        minute = date.minute
                        seconds = date.second
                        mili = date.microsecond
                    except:
                        raise Exception("Could not find DateTime module please import DateTime ")

                    fileopen = open(f"{datee}{hour}{minute}{seconds}{mili}.txt", 'w')
                    filewrite = fileopen.write("1234")
                    fileopen.close()
                    break

                except:
                    raise Exception("error")

        for i in password:
            while i == "w":
                try:
                    try:
                        date = datetime.now()
                        datee = date.date()
                        hour = date.hour
                        minute = date.minute
                        seconds = date.second
                        mili = date.microsecond
                    except:
                        raise Exception("Could not find DateTime module please import DateTime ")

                    fileopen = open(f"{datee}{hour}{minute}{seconds}{mili}.txt", 'w')
                    filewrite = fileopen.write("1234")
                    fileopen.close()
                    break

                except:
                    raise Exception("error")

        for i in password:
            while i == "x":
                try:
                    try:
                        date = datetime.now()
                        datee = date.date()
                        hour = date.hour
                        minute = date.minute
                        seconds = date.second
                        mili = date.microsecond
                    except:
                        raise Exception("Could not find DateTime module please import DateTime ")

                    fileopen = open(f"{datee}{hour}{minute}{seconds}{mili}.txt", 'w')
                    filewrite = fileopen.write("1234")
                    fileopen.close()
                    break

                except:
                    raise Exception("error")

        for i in password:
            while i == "y":
                try:
                    try:
                        date = datetime.now()
                        datee = date.date()
                        hour = date.hour
                        minute = date.minute
                        seconds = date.second
                        mili = date.microsecond
                    except:
                        raise Exception("Could not find DateTime module please import DateTime ")

                    fileopen = open(f"{datee}{hour}{minute}{seconds}{mili}.txt", 'w')
                    filewrite = fileopen.write("1234")
                    fileopen.close()
                    break

                except:
                    raise Exception("error")
        for i in password:
            while i == "z":
                try:
                    try:
                        date = datetime.now()
                        datee = date.date()
                        hour = date.hour
                        minute = date.minute
                        seconds = date.second
                        mili = date.microsecond
                    except:
                        raise Exception("Could not find DateTime module please import DateTime ")

                    fileopen = open(f"{datee}{hour}{minute}{seconds}{mili}.txt", 'w')
                    filewrite = fileopen.write("1234")
                    fileopen.close()
                    break

                except:
                    raise Exception("error")
    except:
        print("error")
